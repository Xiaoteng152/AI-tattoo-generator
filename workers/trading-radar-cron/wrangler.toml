name = "trading-radar-cron"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[triggers]
crons = ["*/15 * * * *"]

[vars]
# 生产 Sync API，例如 https://your-domain.com/api/trading-radar/sync
TRADING_RADAR_SYNC_URL = ""

# CRON_SECRET 通过 wrangler secret put CRON_SECRET 注入，不要写入仓库
